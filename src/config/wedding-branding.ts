/**
 * Wedding branding constants for Checkpoint.
 *
 * The RSVP flow is currently themed for a single wedding event (Caleb × Rachel).
 * Keep these in one place so the creative copy can be changed without touching
 * the RSVP components themselves.
 */
export const WEDDING_BRANDING = {
  monogram: "C · R",
  monogramCompact: "C·R",
  hashtags: ["#CALEBGETSRICH", "#HAPPELYEVERGYAMFI"],
  hashtagLine: "#CALEBGETSRICH #HAPPELYEVERGYAMFI",
} as const;
