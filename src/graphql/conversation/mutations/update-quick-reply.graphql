mutation UpdateQuickReply(
  $id: String!
  $key: String
  $body: String
  $channel: String
  $tags: [String!]
) {
  updateQuickReply(id: $id, key: $key, body: $body, channel: $channel, tags: $tags) {
    id
    key
    body
    channel
    tags
    createdAt
    updatedAt
  }
}
