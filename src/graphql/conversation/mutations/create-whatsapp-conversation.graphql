mutation CreateWhatsappConversation($phoneNumber: String!, $displayName: String) {
  createWhatsappConversation(phoneNumber: $phoneNumber, displayName: $displayName) {
    id
    type
    channel
    lastMessage
    lastMessageAt
    unreadCount
    externalAddress
    externalDisplayName
    participants {
      userId
    }
  }
}
