query ConversationById($id: ID!) {
  conversation(id: $id) {
    id
    type
    channel
    lastMessage
    lastMessageAt
    unreadCount
    externalAddress
    externalDisplayName
    participants {
      userId
    }
  }
}
