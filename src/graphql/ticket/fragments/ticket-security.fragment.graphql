fragment TicketSecurity on TicketPayload {
  id
  deviceId
  devicePublicKey
  deviceActivationAt
  deviceActivationIP

  lastNonce
  nextNonce

  checkedInAt

  revoked
  revokedAt
  revokedBy
  revokedReason
}
