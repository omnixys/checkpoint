query Seats($id: ID!) {
  seats(eventId: $id) {
    ...SeatFull
  }
}

query SeatList($id: ID!) {
  seats(eventId: $id) {
    id
    eventId
    label
    status
    note
    guestId
    invitationId

    number

    section {
      name
      id
    }

    table {
      name
      id
    }
  }
}
