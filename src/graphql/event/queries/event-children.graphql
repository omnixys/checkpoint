query EventChildren($eventId: ID!) {
  eventChildren(eventId: $eventId) {
    ...EventBase
  }
}

query GetSubEventNameList($eventId: ID!) {
  eventChildren(eventId: $eventId) {
    id
    name
    settings {
      id
      startsAt
      endsAt
    }
  }
}
