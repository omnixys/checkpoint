query EventChildren($id: ID!) {
  eventChildren(eventId: $id) {
    ...EventBase
  }
}

query GetSubEventNameList($eventId: ID!) {
  eventChildren(eventId: $eventId) {
    id
    name
    settings {
      id
      startsAt
      endsAt
    }
  }
}
