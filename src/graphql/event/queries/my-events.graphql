# import EventFull from "../fragments/event.fragment.graphql"


query MyEvents {
  myEvents {
    id
    name
    settings {
      startsAt
      endsAt
    }
  }
}

query GetMyEventCalendarData {
  myEvents {
    id
    name
    settings {
      startsAt
    }
  }
}
