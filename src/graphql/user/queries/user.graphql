# import UserFull from "../fragments/user.fragment.graphql"

query User($id: ID!) {
  getUserList(userIds: [$id]) {
    ...UserFull
  }
}

query GetUserName($id: ID!) {
  getUserList(userIds: [$id]) {
    personalInfo {
      firstName
      lastName
    }
  }
}
