mutation BulkApproveInvitations($input: BulkApproveInvitationInput!, $eventEndsAt: DateTime!) {
  bulkApproveInvitations(input: $input, eventEndsAt: $eventEndsAt) {
    id
    status
    approvedAt
  }
}
