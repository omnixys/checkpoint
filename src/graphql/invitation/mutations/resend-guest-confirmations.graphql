mutation ResendGuestConfirmations($invitationIds: [ID!]!, $locale: String) {
  resendGuestConfirmations(invitationIds: $invitationIds, locale: $locale) {
    total
    resent
    skipped
    results {
      invitationId
      resent
      reason
    }
  }
}
