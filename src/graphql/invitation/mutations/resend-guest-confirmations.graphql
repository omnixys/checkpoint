mutation ResendGuestConfirmations($invitationIds: [ID!]!) {
  resendGuestConfirmations(invitationIds: $invitationIds) {
    total
    resent
    skipped
    results {
      invitationId
      resent
      reason
    }
  }
}