fragment InvitationPlusOne on InvitationPayload {
  id
  firstName
  lastName
  email
  plusOneAgeCategory

  guestNote
  selectedInvitedBy

  status
  guestProfileId

  pendingContactId

  rsvpChoice
  rsvpAt

  approvedAt
  approvedByUserId

  invitedByInvitationId
  invitedByUserId

  phoneNumbers {
    ...PhoneNumber
  }
}
