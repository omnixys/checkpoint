fragment InvitationBase on InvitationPayload {
  id
  type
  firstName
  lastName
  eventId
  eventName
  eventEndsAt
  guestProfileId
  email

  status
  createdAt
  updatedAt

  pendingContactId
  phoneNumber
  selectedInvitedBy
  guestNote
  plusOneAgeCategory

  rsvpChoice
  rsvpAt

  approvedAt
  approvedByUserId

  maxInvitees

  invitedByInvitationId
  invitedByUserId
}
